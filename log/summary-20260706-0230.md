# 2026-07-06 0230 — 回退到 POSIX 兼容版本

## 修改的问题
- 迭代5重构引入了 `DFAMachine` 结构体扩展（`has_anchor_start/end`）、`AST_ANCHOR_*` 节点、捕获组崩溃修复等新功能
- 但这些改动导致 test_dfa.c 崩溃（NFA 构造中 `ANCHOR_START` 无子节点时返回 `{NULL,NULL}`，CONCAT 拼接时代码解引用 NULL）
- parser 中 `$` 和 `^` 未正确集成到 chain 解析流程，`abc$` 解析失败返回空 DFA

## 解决的问题
- 已回退到 `90b0ba8`（"优化接口兼容 POSIX" 提交），恢复稳定的 POSIX 兼容基线
- 移除了迭代5引入的所有改动（dfa.h/parser.h/capture.c/dfa.c/matcher.c/nfa.c/parser.c/posix_compat.c/test_dfa.c/test_posix_compat.c）

## 未解决的问题
1. 迭代5的 `^` / `$` 锚定 NFA 构造 bug（ANCHOR_START 无子节点时返回空片段）
2. 迭代5的 parser 中 `^` / `$` 未纳入 chain 解析
3. grep 开源测试集 90%+ 通过
4. DFA 匹配速度 ≥ POSIX 80%
5. NFA→DFA 示例图 + 性能对比表

## 下一步
1. 提交回退，建立干净的 POSIX 基线
2. 在基线上逐个修复迭代5发现的 bug
3. 重新实现锚定支持（先修 parser，再修 NFA builder）
