# 2026-07-06 00:00 — 审计修复完成

## 修复的问题

### 严重

1. **posix_compat.c: REG_NOSUB 标志误用** — 从检查 `prog->re_flags` 改为 `eflags` ✅
2. **dfa_dump_dot done_targets[256] 越界** — 改为动态分配 `calloc(state_count+1)` ✅

### 中等

3. **capture.c: match_sub_dfa_greedy 空匹配** — `last_accept > text_start` 改为 `>=` ✅
4. **posix_compat: pmatch[1..nsub] 未初始化** — 显式设置为 -1 ✅
5. **main.c 使用高级 API** — 替换为 `regex_compile`/`regex_match`/`regexec` ✅

### 轻微

6. **posix_compat.h 缺少 regoff_t 定义** — 添加 `<stdint.h>` 和 `typedef intptr_t regoff_t` ✅
7. **hopcroft.c goto oom 资源清理顺序** — 调整为正确顺序 ✅
8. **unused variable bracket_negated** — 移除 ✅
9. **printf format 不匹配 regoff_t** — `%d` 改为 `%lld` ✅

## 修复后状态

- 编译：✅ 零警告
- 匹配测试：✅ 全部通过
- POSIX 兼容层：✅ regexec 正确返回 rm_so/rm_eo
- 高级 API：✅ regex_compile/match/search 正常工作

## 未完成项（需更大改动）

1. 捕获组信息接入 POSIX 层（需扩展 API 传递 CapturedMatch）
2. 错误恢复机制（需修改 parser）
3. 性能基准测试
4. 测试集覆盖率
