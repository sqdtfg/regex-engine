# 2026-07-06 02:00 — 迭代5中期总结

## 已完成

### 捕获组崩溃修复
- `dfa_match_captured` use-after-free → 通过 `ast_clone()` 深拷贝子AST解决
- `match_sub_dfa_greedy` 改为区间内多偏移扫描，取最长匹配
- `dfa_from_ast_with_groups` 中 `collect_group_metas_recursive` 正确处理相邻捕获组

### POSIX 捕获组传递
- `regex_prog_t` 新增 `capture_dfa` 字段（堆分配 `DFAMachine*`）
- `regcomp` 构建 capture-aware DFA，`regexec` 使用 `dfa_match_captured` 填充 `pmatch[1..nsub]`
- `regfree` 正确释放 capture DFA
- `(abc)(def)` 测试：pmatch[1]=(0,3), pmatch[2]=(3,6) ✅

### `^` 和 `$` 锚定语法
- 新增 `AST_ANCHOR_START` / `AST_ANCHOR_END` 节点类型
- `DFAMachine` 新增 `has_anchor_start` / `has_anchor_end` 字段
- `NFAGraph` 保持原样（锚定不改变NFA结构）
- `matcher.c` 中 `dfa_match` / `dfa_match_all` 根据锚定标志约束搜索范围
- `dfa_from_nfa` 传播锚定标志

### 测试覆盖
- 547 项测试全部通过（9个测试套件）
- 63 项性能基准测试运行正常
- 新增 `test_matcher.c` (55), `test_api.c` (67), `test_capture.c` (42), `test_posix_compat.c` (73), `test_benchmark.c`

## 当前阻塞问题

**`DFAMachine` 结构体扩展导致 test_dfa.c 崩溃**
- 新增 `has_anchor_start` / `has_anchor_end` 两个 int 字段
- `do_build("a{3}")` 在 `CHECK_FALSE(dfa.states[s1].is_accept, ...)` 处 segfault
- 移除这两个字段后测试通过
- 需要确认 `dfa_from_nfa` 中的 `DFAMachine dfa = {0}` 是否正确初始化了新字段
- 可能原因：CMake 缓存了旧的 object 文件，头文件变更未触发重新编译

## 剩余验收标准

1. ❌ grep 开源测试集 90%+ 通过
2. ❌ DFA 匹配速度 ≥ POSIX 80%
3. ❌ NFA→DFA 示例图 + 性能对比表

## 下一步

1. 修复 `DFAMachine` 结构体扩展导致的崩溃（可能需要全量 clean rebuild）
2. 编写 grep 开源测试集适配
3. 编写 DFA vs POSIX 性能对比测试
4. 生成 NFA→DFA 示例图和性能对比表
